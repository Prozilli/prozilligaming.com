# ============================================================================
# PROZILLIGAMING.COM - Environment Variables
# ============================================================================
# These variables are used during build and runtime
# Configure in Cloudflare Pages: Settings > Environment Variables

# ── Admin Authentication ──
# IMPORTANT: This is only a UI gate - real security is in the API
# Consider using Cloudflare Access for proper zero-trust authentication
NEXT_PUBLIC_ADMIN_PASSWORD=your_secure_password_here

# ── PRISMAI Backend URLs ──
# Points to PRISMAI servers (configure these in Cloudflare Pages)
PRISMAI_CORE_URL=http://65.109.100.181:5084
PRISMAI_ANALYTICS_URL=http://5.161.119.210:5018

# ── Build Configuration ──
# These are handled by Next.js automatically
# NODE_ENV=production
