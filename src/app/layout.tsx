import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Prozilli Gaming — Live. Create. Dominate.",
    template: "%s | Prozilli Gaming",
  },
  description:
    "Multiplatform live streaming across YouTube, Twitch, Kick, Trovo, Facebook and more. Part of the Prozilli Entertainment ecosystem.",
  metadataBase: new URL("https://prozilligaming.com"),
  openGraph: {
    siteName: "Prozilli Gaming",
    type: "website",
    locale: "en_US",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navbar />
        <main className="min-h-screen pt-16">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
